{
  "name": "mantenimiento-industrial-total",
  "version": "1.0.0",
  "description": "Sistema de Gestión de Mantenimiento Industrial",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "copy:frontend": "shx rm -rf backend/public && shx cp -r frontend/dist backend/public",
    "build:total": "npm run build:frontend && npm run copy:frontend",
    "start": "cd backend && node index.js"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
